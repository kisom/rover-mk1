project Build extends "avr_app.gpr" is

   for Main use ("rover");

   for Object_Dir use "obj";
   for Exec_Dir use "build";

   for Source_Dirs use ("src/hardware", "src/rosa", "src/");

end Build;
